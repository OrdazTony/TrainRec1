# Frontend environment variables
# Set this to your Render backend URL in production
VITE_API_URL=https://your-render-backend.onrender.com

# Optional on Vercel: only needed for Google-hosted gym photos and Street View images.
# The gym list itself now loads through the Render backend.
VITE_GOOGLE_MAPS_KEY=your-browser-restricted-google-maps-key
